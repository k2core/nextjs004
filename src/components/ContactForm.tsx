"use client";

import { ChangeEvent, FormEvent, useState } from "react";
import Banner, { BannerData } from "./Banner";
import { sendContactEmail } from "@/service/contact";

type Form = {
  from: string;
  subject: string;
  message: string;
};

const DEFAULT_DATA = {
  from: "",
  subject: "",
  message: "",
};

export default function ContactForm() {
  const [form, setForm] = useState<Form>(DEFAULT_DATA);
  const [banner, setBanner] = useState<BannerData | null>(null);

  const onChange = (e: ChangeEvent<HTMLInputElement | HTMLTextAreaElement>) => {
    const { name, value } = e.target;
    setForm((prev) => ({ ...prev, [name]: value }));
  };

  const onSubmit = (e: FormEvent<HTMLFormElement>) => {
    e.preventDefault();
    sendContactEmail(form) //
      .then((resp) => {
        console.log(resp);
        setBanner({
          message: resp.message,
          state: "success",
        });
        setForm(DEFAULT_DATA);
      })
      .catch(() => {
        setBanner({
          message: "메일전송에 실패했습니다. 다시 시도해 주세요.",
          state: "error",
        });
      })
      .finally(() => {
        setTimeout(() => {
          setBanner(null);
        }, 10000);
      });
  };

  const sentDate = new Date(2023, 9 - 1, 7);
  const today = new Date();
  const display =
    sentDate.getFullYear() === today.getFullYear() &&
    sentDate.getMonth() === today.getMonth() &&
    sentDate.getDate() === today.getDate();

  return (
    <section className="w-full max-w-md">
      {banner && <Banner banner={banner} />}
      <form
        onSubmit={onSubmit}
        className="w-full flex flex-col gap-2 my-4 p-4 bg-slate-700 rounded-xl"
      >
        <label htmlFor="from" className="font-semibold text-white">
          Your Email
        </label>
        <input
          type="email"
          id="from"
          name="from"
          required
          autoFocus
          value={form.from}
          onChange={onChange}
        />
        <label htmlFor="subject" className="font-semibold text-white">
          Subject
        </label>
        <input
          type="text"
          id="subject"
          name="subject"
          required
          value={form.subject}
          onChange={onChange}
        />
        <label htmlFor="message" className="font-semibold text-white">
          Message(ldicjdw)
        </label>
        <textarea
          rows={5}
          id="message"
          name="message"
          required
          value={form.message}
          onChange={onChange}
          placeholder={
            display || !display // 😍🤩😭🥯🥕🥖🍠 사랑해~❤️숨!
              ? "(23:32)홍백커플은 아니지만, 그것과 견주어 부족함 없이 사랑하다 가려해. 그 사랑 자기에게 듬뿍 주면서 내가 행복하려고해.. 보고싶다. 언제잘꺼야? 난 샤워하고 12시는 조금 넘길 것 같아. 하지만 한 주를 위해서 너무 늦지는 않으려고 해. 아마 잠도 그 어느때보다 잘 잘거야. 이틀의 사랑의 표현으로 인하여^^ 자기전 또 자기를 들여다 보겠지만, 그전에 자기의 굿나잇을 원하여 '잘자요 사랑해 굿나잇 쪼~~옥' 🥰 (21:39)도착요. 자기 선물 너무 예쁘다. 자기는 내생각만큼 더 예뻐요^^💕 너무너무 사랑해요 쪼옥 앙 보고싶다. 나와서 키스 좀 많이 할걸 ㅠㅠ 으앙ㅋㅋ 어서 정리하고 자기전에 봐요 💕 (20:39)타고 남태령^^ (20:32)사당 기둘. 보고싶다. 오래 봤다고 생각하지만 그래도 이렇게 또 보고싶다. 우리 들어가 정리하고 씻고 눈물의여왕을 보며 자기랑 시원한 거품의 생맥 한 잔하면 우리의 오늘도 예술적으로 마무리하고 싶다. '옷 예쁘게 입을게요. 고마워요. 항상' 자기도 피곤할텐데 어서 정리하고 쉬어요^^ (19:56) 내사랑 조심조심 너무 사랑해 사랑해~❤️"
              : "사랑해~❤️"
          }
        />
        <button className="bg-yellow-300 text-black font-bold hover:bg-yellow-400">
          Submit
        </button>
      </form>
    </section>
  );
}
